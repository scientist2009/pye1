# not a GNU package. You can remove this line, if
# have all needed files, that a GNU package needs
SUBDIRS = engine test tools

EXTRA_DIST = Makefile.cvs pye.pc.in

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = pye.pc
